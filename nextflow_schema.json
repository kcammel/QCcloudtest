{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/princessmaximacenter/sturgeon_classifier//nextflow_schema.json",
  "title": "princessmaximacenter/sturgeon_classifier pipeline parameters",
  "description": "Nextflow run princessmaximacenter/nextflow_pipeline -profile {sturgeon,qc} -params-file {params.[json|yaml]}",
  "type": "object",
  "properties": {
    "pipeline": {
      "type": "string",
      "description": "Set to a specific workflow after setting profile",
      "enum": ["sturgeon", "qc"],
      "hidden": true
    }
  }
}
